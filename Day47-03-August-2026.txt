# Day 28 — Debug Day

Fix these intentionally broken files:
## Ansible — Broken

- name Install nginx
 hosts all
 tasks:
  - name missing
    yum:
      name nginx
      state present

## Ansible correct file
---
- name: Install nginx
  hosts: all
  become: yes

  tasks:
    name: Install nginx
    yum:
      name: nginx
      state: present

## Terraform — Broken

resource "aws_s3" "BadBucket" {
bucket = pathnex-bucket
acl public-read
}

## Terraform correct file
resource "aws_s3_bucket" "goodbucket" {
    bucket = "pathnex-bucket"
}

resource "aws_s3_bucket_acl" "goodbucketacl" {
    bucket = aws_s3.goodbucket.id
    acl = "public-read"
}

## Kubernetes — Broken

apiVersion v1
kind Pod
metadata:
 name brokenpod
spec:
 containers:
  -name app
   image nginx

## Corrected Pod
apiVersion: v1
kind: Pod
metadata:
  name: pathnex-pod
spec:
  containers:
    - name: app
      image: nginx:latest

# Rollback Strategies

## Jenkins Pipeline — Automatic Rollback
You will learn how to **rollback automatically if deployment fails**.
pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Pathnex"
        TEAM = "DevOps"
        ENV = "prod"
    }
    stages {
        stage('Deploy') {
            steps {
                script {
                    try {
                        sh 'echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"'
                        sh 'exit 1' // simulate failure
                    } catch (Exception e) {
                        echo "Deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"
                    }
                }
            }
        }
    }
}

## Corrected pipeline
pipeline {
    agent any
    environment {
        TEAM = "DevOps"
        ENV = "prod"
        INSTITUTE_NAME = "Pathnex"
    }

    stages {
        stage('Deploy') {
            steps {
                script {
                    try {
                        sh 'echo "Deploying $INSTITUTE_NAME app in $ENV env by $TEAM team"'
                        sh 'exit 1' //Simulate failure
                    } catch(Exception e) {
                        echo 'Deployment failed. Rollback of $INSTITUTE_NAME app in $ENV env'
                    }
                }
            }
        }
    }
}

## GitLab CI — Rollback Strategy
You will learn how to **simulate rollback in GitLab CI with tags**.

stages:
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"
  TEAM = "DevOps"
  ENV = "prod"

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"
    - exit 1
  after_script:
    - echo "Deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"

Corrected and Practice
stages:
  - deploy

variables:
  INSTITUTE_NAME = "Pathnex"
  ENV = "Prod"
  TEAM = "DevOps"

deploy:
  stage: deploy
  image: alpine:latest
  script:
    echo "Deploying in $INSTITUTE_NAME app by $TEAM team in $ENV env"
    exit 1
  after_script:
    echo "Deployment failed. Rollback $INSTITUTE_NAME app in $ENV"