# Day 21 — SPEED TYPING (LEVEL 2)

You must rewrite all FOUR from scratch:

1. Ansible:
   - Install nginx
   - Start + enable service

---
- name: Install and start nginx
  hosts: all
  become: yes

  tasks:
   - name: Install nginx
     yum:
       name: nginx
       state: present

    - name: Start nginx service
      service:
        name: nginx
        service: started
        enabled: yes

2. Terraform:
   - Create EC2 (choose any Pathnex-approved type)
   - Tag it with:
       Owner: PathnexStudent

resource "aws_instance" "PathnexEC2" {
    ami = "ami-12345fsfgg55555"
    instance_type = "m5.xlarge"

    tags = {
        Name = "PathnexStudent"
    }
}

3. Kubernetes:
   - Deployment with 3 replicas
   - Image: nginx

apiVersion: appss/v1
kind: Deployment
metadata:
  name: pathnex-deployment
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable:1

  selector:
    matchLabels:
      app: pathnex-app
  template:
    metadata:
      labels:
        app: pathnex-app
    spec:
      containers:
        - name: nginx-backend
          image: nginx:latest

4. Shell:
   - Print date, uptime, disk usage

#!/bin/bash
echo "Print date $(date)"
echo "Print uptime $(uptime)"
echo "Print disk usage $(df -h)"

# Multi-Environment Pipeline
## Jenkins Pipeline — Deploy to Multiple Environments
You will learn how to **define multiple environment deployments in one pipeline**.

pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Pathnex"
    }
    stages {
        stage('Deploy to Dev') {
            steps {
                sh 'echo "Deploying for $INSTITUTE_NAME in Dev environment"' 
            }
        }
        stage('Deploy to Staging') {
            steps {
                input message: "Approve staging deployment?"
                sh 'echo "Deploying for $INSTITUTE_NAME in Staging environment"'
            }
        }
        stage('Deploy to Production') {
            steps {
                input message: "Approval for Production deployment"
                sh 'echo "Deploying for $INSTITUTE_NAME in Production environment"'
            }
        }
    }
}

## GitLab CI — Multi-Environment
You will learn how to **define multiple environment deployments in GitLab CI**.

stages: 
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"

deploy-dev:
  stage: deploy
  image: maven:3.8.1-jdk-17
  script:
    - echo "Deploying for $INSTITUTE_NAME in Dev environment"

deploy-staging:
  stage: deploy
  image: maven:3.8.1-jdk-17
  script:
    - echo "Deploying for $INSTITUTE_NAME in Staging environment"
  when: manual

deploy-prod:
  stage: deploy
  image: maven:3.8.1-jdk-17
  script:
    - echo "Deploying for $INSTITUTE_NAME in Production environment"
  when: manual